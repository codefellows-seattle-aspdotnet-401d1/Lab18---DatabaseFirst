@model IEnumerable<Lab18Cameorn.Models.Destinations>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Country)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Climate)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Climate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DestinationsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DestinationsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DestinationsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
