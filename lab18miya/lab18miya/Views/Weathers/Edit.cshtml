@model lab18miya.Models.Weather

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Weather</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WeatherID" />
            <div class="form-group">
                <label asp-for="WeatherType" class="control-label"></label>
                <input asp-for="WeatherType" class="form-control" />
                <span asp-validation-for="WeatherType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
